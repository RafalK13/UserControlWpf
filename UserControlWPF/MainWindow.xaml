<Window x:Class="UserControlWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UserControlWPF"
        xmlns:controls="clr-namespace:WpfControlLibraryRaf;assembly=WpfControlLibraryRaf"
        mc:Ignorable="d"
        x:Name="win"
        Title="MainWindow" Height="612" Width="314">

    <Window.DataContext>
        <local:DaneRaf/>
    </Window.DataContext>

    <Window.Resources>
        <local:StringToInt x:Key="convStrToInt"/>

        <controls:DataGridRaf x:Key="controlRaf" />
        <controls:UserControlRaf1 x:Key="UserRaf1"/>
        <local:DaneRaf x:Key="daneRaf" />
        <Style TargetType="TextBox">
            <Setter Property="FontSize" Value="30"/>
        </Style>
    </Window.Resources>

    <StackPanel Orientation="Vertical">

        <TextBox Height="50"
                Text="{Binding selectedItem}"/>

        <TextBox Height="50"
                 Text="{Binding ElementName=controlRaf, Path=selectedValueRaf}"/>

        <DataGrid   Name="d"
                    ItemsSource="{Binding dtSourceList}"
                    SelectedValue="{Binding selectedItem}"
                    SelectedValuePath="id"
                    HeadersVisibility="None"
                    AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding id}"
                                    Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
   
        <controls:DataGridRaf   Name="controlRaf" 
                                dataGridSource="{Binding Path=DataContext.dtSourceRaf, ElementName=win}"
                                selectedValueRaf="{Binding Path=DataContext.selectedItem,  ElementName=win, Converter={StaticResource convStrToInt}, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                selectedValuePathRaf="id"
                                colName="name" Height="321" 
                                />

        <!--<controls:UserControl3 tekstUserRaf="{Binding ElementName=t1, Path=Text, Mode=TwoWay}"/>
        <TextBox Name="t1" Height="50" Background="Aquamarine"/>-->

        <!--<TextBox    Background="LightPink"   
                    Height="50"
                    Text="{Binding ElementName=controlRaf, Path=selectedValueRaf}"/>
        <TextBox    Background="Bisque"
                    Height="50"
                    Text="{Binding tekst, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                    Name="ID"/>

        <DataGrid ItemsSource="{Binding Path=DataContext.dtSourceRaf, ElementName=win}"
                  SelectedValue="{Binding tekst, Converter={StaticResource convStrToInt}}"
                  SelectedValuePath="age"
                  
                  />-->

        <!--selectedValueRaf="{Binding ElementName=ID, Path=Text, Converter={StaticResource convStrToInt}}"-->


        <!--<DataGrid ItemsSource="{Binding Path=DataContext.dtSourceRaf, ElementName=win}"/>-->

        <!--<TextBox Margin="0,0,0,0"   Text="{Binding drvSelected[0]}" VerticalAlignment="Top"/>
        <Button Margin="0,50,0,0"   Height="50" Width="200" VerticalAlignment="Top" Click="Button_Click"/>
        <controls:UserControlRaf1   Margin="0,110,0,0"
                                    VerticalAlignment="Top"
                                    x:Name="control1Raf"
                                    dataRowView="{Binding ElementName=win, Path=DataContext.drvSelected, Mode=TwoWay}"
                                    dataTable="{Binding ElementName=win, Path=DataContext.dtSourceRaf}" 
                                    />-->


        <!--<TextBox Margin="0,0,0,0" Text="{Binding tekst}" VerticalAlignment="Top"/>
        <TextBox Margin="0,50,0,0" Text="{Binding drvSelected[0]}" VerticalAlignment="Top"/>-->
        <!--dataRowView="{Binding drvSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                        dataTable="{Binding dtSourceRaf}" 
                                        />-->
    </StackPanel>
</Window>
